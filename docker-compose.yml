version: "3.8"

services:

  mysql-income-db:
    image: mysql:8.0
    container_name: mysql-income-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: income_service_db
    ports:
      - "3308:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - microservices-net

  income-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: income-service
    ports:
      - "8081:8081"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      JAVA_TOOL_OPTIONS: "-Dspring.profiles.active=docker"
    depends_on:
      mysql-income-db:
        condition: service_healthy
    networks:
      - microservices-net

networks:
  microservices-net:
    external: true

volumes:
  mysql-data: